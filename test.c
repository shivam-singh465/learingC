#include <stdio.h>

int main()
{
    //  sum and the average of tow numbers
    // int num1, num2, average, sum;
    // num1 = 10;
    // num2 = 20;

    // sum = num1 + num2;
    // average = sum / 2;

    // printf(" the some of  num1 %d and num2 %d = %d and the average = %d\n", num1, num2, sum, average);

    // even or odd checker

    // int number;
    // printf("enter the number you want to find the even or odd : ");
    // scanf("%d", &number);
    // if (number % 2 == 0)
    // {
    //     printf("the number %d is the even number\n", number);
    // }
    // else
    // {
    //     printf("the number %d is the odd number\n", number);
    // }

    // simple calculator who calcule the only + - * /

    // int number1, number2, result;
    // char operater;

    // printf("enter the fist number for calculation ");
    // scanf("%d", &number1);
    // printf("enter the second number for calculation ");
    // scanf("%d", &number2);
    // printf("enter the operator for calculation ");

    // scanf("%d", &operater);
    // if (operater == "+")
    // {
    //     result = number1 + number2;
    // }
    // else if (operater == "-")
    // {
    //     result = number1 - number2;
    // }
    // else if (operater == "*")
    // {
    //     result = number1 * number2;
    // }
    // else if (operater == "/")
    // {
    //     result = number1 / number2;
    // }
    // else
    // {
    //     printf("only use the  + , -, * , /");
    // }
    // printf("the result is %d", result);

    // write the program to print "hello" students on the screen

    // int num1, num2, result;
    // printf("enter the first number :");
    // scanf("%d", &num1);

    // printf("enter the second number :");
    // scanf("%d", &num2);

    // result = num1 + num2;
    // printf("the sum of %d and %d is %d \n", num1, num2, result);

    // int num, square;
    // printf("enter the number to find the square");
    // scanf("%d", &num);
    // square = num * num;
    // printf("the square of your number %d is : %d", num, square);

    // int height, width, area;
    // printf("enter the height of the square : ");
    // scanf("%d", &height);
    // printf("enter the width of the square : ");
    // scanf("%d", &width);
    // area = height * width;
    // printf("the area of square with height and with %d %d : %d", height, width, area);

    // float radius, pi = 3.14, area;

    // printf("enter the radius of the circle :");
    // scanf("%f", &radius);
    // area = pi * radius * radius;
    // printf("area of the circle is %f ", area);

    // int num1, num2, num3, average;
    // printf("enter the first number");
    // scanf("%d", &num1);
    // printf("enter the second number");
    // scanf("%d", &num2);
    // printf("enter the third number");
    // scanf("%d", &num3);

    // average = (num1 + num2 + num3) / 3;
    // printf("avearge of your number is %d\n", average);

    // write a program to calulate circumference of a cirle

    // float radius, circum, pi = 3.14;
    // printf("enter the radius of the circlue ");
    // scanf("%f", &radius);

    // circum = 2 * radius * pi;
    // printf("the circumference of the circlue is : %f\n", circum);

    // write the program to calculete the simple interest;
    // float princple, rate, time, interest;
    // printf("enter the princple amount");
    // scanf("%f", &princple);
    // printf("enter the rate of interest");
    // scanf("%f", &rate);
    // printf("enter the time ");
    // scanf("%f", &time);
    // interest = (princple * rate * time) / 100;
    // printf("the interest is %f\n", interest);

    // write the program to calculate the volume of a cuboid
    // int length, height, width, volume;
    // printf("enter the length of the cuboid ");
    // scanf("%d", &length);
    // printf("enter the width of the cuboid ");
    // scanf("%d", &width);
    // printf("enter the height of the cuboid ");
    // scanf("%d", &height);

    // volume = height * width * length;
    // printf("the volume of cuboid is : %d", volume);

    // write a program to ask user about the cost price and selling price banana per dozen. Calculate the profit or loss earned upon selling 25 bananas.

    // total banana = 2 + 1 / 12

    // float cost_p, selling_p, profit, loss, result;
    // printf("enter the cost price of banana per dozen : ");
    // scanf("%f", &cost_p);
    // printf("enter the selling price of banana per dozen : ");
    // scanf("%f", &selling_p);

    // if (cost_p > selling_p)
    // {
    //     loss = (cost_p - selling_p) / 12;
    //     result = loss * 25;
    //     printf("loss : %f", result);
    // }
    // else
    // {
    //     profit = (selling_p - cost_p) / 12;
    //     result = profit * 25;
    //     printf("profit : %f", result);
    // }

    // ascii value of a character

    // char asciiCode;
    // printf("enter the character to print its ascii :");
    // scanf(" %c", &asciiCode); // Notice the space before %c
    // printf("%d \n", asciiCode);

    // character from ascii value
    // int val;
    // printf("enter the ascii code to print its character ; ");
    // scanf(" %d", &val);
    // printf("character is : %c \n", val);

    // last digit of a number

    // int num;
    // printf("enter the number ");
    // scanf(" %d", &num);
    // printf("the last digit of your number is %d", num % 10);

    // number without last digit
    //   int num;
    //  printf("enter the number ");
    //  scanf(" %d", &num);
    //  printf("the  number without your last digit %d", num / 10);

    // char ch1, ch2, ch3,first,second,third;
    // printf("enter the 3 character ");
    // scanf("%c %c %c", &ch1, &ch2, &ch3);
    // printf("%d",ch1<ch2);
    // if(ch1<ch2 && ch1<ch3){
    //     first = ch1;
    //     if(ch2<ch3){
    //         second = ch2;
    //         third = ch3;
    //     }
    //     else{
    //         second = ch3;
    //         third = ch2;
    //     }
    // }
    // else if(ch2<ch1 && ch2<ch3){
    //     first = ch2;
    //     if(ch1<ch3){
    //         second = ch1;
    //         third = ch3;
    //     }
    //     else{
    //         second = ch3;
    //         third = ch1;
    //     }
    // }
    // else{
    //     first = ch3;
    //     if(ch1<ch2){
    //         second = ch1;
    //         third = ch2;
    //     }
    //     else{
    //         second = ch2;
    //         third = ch1;
    //     }
    // }
    // printf("the order is : %c %c %c",first,second,third);




    printf("\n");
    return 0;
}